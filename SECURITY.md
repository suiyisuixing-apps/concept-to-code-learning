# 安全与隐私边界

当前程序是单用户本地学习工作台，桌面入口只监听 127.0.0.1，没有账号认证、同步或遥测，不适合作为多租户或公网服务。旧版 Fixture 接口保留用于兼容，新版 /api/learning/v1 使用真实文档、已配置的模型和来源 Provider；不可用时明确失败，不替换为模拟成功。

文档、批注、会话与讲解保存在本机独立数据目录。解释请求会记录会话与讲解，但只有用户显式保存才创建个人笔记。旧版笔记保持追加，新版笔记允许带修订号的编辑和明确确认的删除；编辑只更改用户内容，冻结的讲解与来源快照保持不变。

本地模型接收问题、选区、必要上下文、近期对话和已核验的代码。远端模型必须由主机显式配置并授权，使用 HTTPS；此时这些内容会发送至该端点。模型选择不会把主机配置的密钥转发给另一端点，不读取环境中的通用 GitHub 管理凭据。

用户选择 GitHub 来源模式后允许公开仓库请求。自动检索由所选模型提炼短技术词，并对地址、路径、长标识符等做结构检查；这不是通用的机密词识别器。涉及保密材料时使用仅文档或授权本地仓库模式。公开源码必须核对仓库可见性、固定版本、文件字节与许可；仅固定版本的公开响应写入有限缓存。请求不跟随重定向，也不使用环境代理。

文档与代码是不可信内容，不能覆盖系统指令或授权执行命令。只读取显式授权的本地根目录，核对目录身份，阻止越界路径，保持原文件和来源仓库不可变。文档在有限时长的独立解析进程中处理，禁用网络；这不等于通用的操作系统安全沙箱。搜索结果不能直接标为已核验。模型不运行源代码，最终回答不用不必要的本地绝对路径。

代码引用最小化，固定 Commit、文件、符号、行号、哈希与许可证。未知许可证不大量复制。执行是单独授权的隔离步骤；未运行不得 VERIFIED_RUNNABLE。错误不伪造成功、不清空或覆盖笔记。

不要在 Issues 中粘贴密钥或学校/企业资料。通过已授权的私有协作流程报告可复现问题，附去敏输入与最小证据；本项目不自动发送外部消息或安装付费安全服务。
