# 比赛来源记录

2026-09-07：根据用户给出的 Phase 0 提示创建本项目。初始 Skill、Schema、CLI、
合成培训段落、示例函数、pytest、协作文档为本轮新写工程资产，使用 Codex 辅助生成并实际验收。
由于正式比赛起止与赛前规则未提供，所有这些资产归类为“赛前工程基础”，不冒充比赛期间新增代码。

赛前标签：`pre-rules-v0.1.0`。用 `git rev-list -n 1 pre-rules-v0.1.0` 获取不可歧义的基线。
正式规则确认后记录时间、官方来源、允许复用范围以及比赛开发的起点 SHA。
后续新增代码通过独立 PR 记录作者、Issue、测试和相对基线的差异。

仅只读检查三个已有仓库的元数据、固定 HEAD 的模块入口和许可文件；没有复制实现代码、
真实课程资料、企业资料、模型或数据库。详见 reuse-ledger。

外部工程资料：GitHub 官方 Actions/分支保护文档、python-jsonschema 官方接口文档。
安装的依赖保留各自许可证，未将依赖源码或模型导入本仓库。产品相邻类别仅作边界提示。
