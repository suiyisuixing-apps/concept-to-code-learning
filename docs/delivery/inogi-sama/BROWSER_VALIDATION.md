> 历史记录（成员原始交付）。保留原有结果与限制，不用于声明当前候选已验收；当前范围见 [../lead/REVIEW_CORRECTIONS.md](../lead/REVIEW_CORRECTIONS.md)。

# 浏览器验收记录

日期：2026-09-09；本机原生 Windows；API `http://127.0.0.1:8766`。

## 测试材料与网络记录

上传自制 UTF-8 `中文 emoji 学习材料.md`，包含中文、emoji、Python code fence 和
`javascript:` 危险链接。服务日志记录：文档上传 `201`、units `200`、Session Context `200`；
页面只显示链接文字，没有可点击危险 URL。Source/Tutor 未接入时显示“部分能力可用”和
“模型尚未配置”，没有固定答案回退。

## 默认桌面视口

- 三栏分别为文档、学习助手、来源/笔记。
- 文件选择、状态、四级切换、来源授权和主操作均可聚焦。
- 文档标题、段落、代码块和能力边界可读，无文字溢出或控件重叠。

## 390 x 844 窄屏

- 顶栏改为两行；文档选择占完整宽度。
- 三栏按文档、助手、来源顺序纵向排列。
- 导入按钮、单元导航和搜索输入没有相互覆盖。
- 代码块使用独立水平滚动，不撑破页面；正文和下一栏不被遮挡。
- 测试完成后已重置浏览器 viewport。

截图通过 Codex in-app browser 的 `getAXStateAndScreenshot` 在该视口现场检查；截图留在本次
验收记录中，不把浏览器会话或本地数据目录作为仓库资产提交。测试上传与数据目录已清理。
