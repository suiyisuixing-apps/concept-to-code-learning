# 非目标

比赛 MVP 不包含员工考核、LMS、强制练习、隐藏评分、自动判断掌握程度、仅针对后端工程师、只连接一家企业私有仓库、以文档漂移为核心或自动修改来源代码。

不默认使用云端模型、遥测、账号、同步或外网访问。不复制完整第三方仓库，不上传学校/企业材料，不引入付费服务。LLM Security Lab 不是唯一演示来源，当前用公开 FastAPI 的三行最小引用。

漂移任务 #21/#34 为 post-MVP/P2，不阻塞 M4；#25/#26/#33 保留历史并以 not planned 关闭。

本轮完整学习版增加四格式、发现式检索、四档追问/比较和来源笔记，但不强制 OCR、任意第三方代码执行、模型训练、复杂多租户或付费系统。DGX 部署包属于 Tutor 完整交付，DGX 实机验收按实际硬件单列。所有既有历史 Issues/Tags 保留，新范围在完整模块 Issue 发布。
