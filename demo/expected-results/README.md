# Expected results

The example prints exactly `active=True inactive=False`. Its pytest must exit zero.
The mapping must name `can_view_profile`, its actual AST line span, and
`test_profile_requires_active_user`. Report envelopes must remain FIXTURE / SCAFFOLD_DEMO.
Compare semantic fields and actual hashes, not timing, temporary paths, or commit IDs.
No pretrained output stands in for execution evidence; reports are generated locally.
